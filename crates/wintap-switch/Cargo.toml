[package]
name = "wintap-switch"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
wintap-switch-core = { path = "../wintap-switch-core" }

[lints.rust]
unsafe_op_in_unsafe_fn = "forbid"
